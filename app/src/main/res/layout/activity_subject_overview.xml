<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SubjectOverviewActivity"
    android:background="#1A2A44">

    <!-- First Horizontal Scroll View (Top) -->
    <android.widget.HorizontalScrollView
        android:id="@+id/topScrollView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- LinearLayout inside HorizontalScrollView for Horizontal List -->
        <LinearLayout
            android:id="@+id/topScrollContent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- Example items for the first horizontal scroll view -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Item 1"
                android:padding="16dp"
                android:textColor="#FFFFFF"
                android:textSize="18sp"
                android:background="#3E4A59"
                android:layout_marginEnd="16dp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Item 2"
                android:padding="16dp"
                android:textColor="#FFFFFF"
                android:textSize="18sp"
                android:background="#3E4A59"
                android:layout_marginEnd="16dp"/>

            <!-- Add more items as needed -->

        </LinearLayout>

    </android.widget.HorizontalScrollView>

    <!-- Second Horizontal Scroll View (Bottom) -->
    <android.widget.HorizontalScrollView
        android:id="@+id/bottomScrollView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/topScrollView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- LinearLayout inside HorizontalScrollView for Horizontal List -->
        <LinearLayout
            android:id="@+id/bottomScrollContent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- Example items for the second horizontal scroll view -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Subject 1"
                android:padding="16dp"
                android:textColor="#FFFFFF"
                android:textSize="18sp"
                android:background="#3E4A59"
                android:layout_marginEnd="16dp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Subject 2"
                android:padding="16dp"
                android:textColor="#FFFFFF"
                android:textSize="18sp"
                android:background="#3E4A59"
                android:layout_marginEnd="16dp"/>

            <!-- Add more items as needed -->

        </LinearLayout>

    </android.widget.HorizontalScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
